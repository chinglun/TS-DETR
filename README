# TS-DETR: Traffic Sign Detection based on Positive and Negative Sample Augmentation

## Introduction
TS-DETR is a traffic sign detection model built on DETR, incorporating positive and negative sample augmentation techniques to improve detection performance, especially in imbalanced datasets.

## Installation
TS-DETR is implemented using [MMDetection v2.25.3](https://github.com/open-mmlab/mmdetection/releases/tag/v2.25.3) and [MMCV v1.5.0](https://github.com/open-mmlab/mmcv/releases/tag/v1.5.0). To install the required dependencies, use the following command:

```bash
pip install -r requirements.txt
